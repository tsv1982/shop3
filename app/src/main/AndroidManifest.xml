<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tsv.shop3">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".View.ListTelUserActivity"></activity>
        <!-- <activity android:name=".View.LoadActivity"></activity> -->
        <activity android:name=".View.MenedgerActivity" />
        <activity android:name=".View.AdminArchiveActivity" />
        <activity android:name=".View.ArchiveActivity" />
        <activity android:name=".View.СhangeProduct" />
        <activity android:name=".View.AddProduct" />
        <activity android:name=".View.Basket" />
        <activity android:name=".View.MainActivity" />
        <activity android:name=".View.LoadActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>